# Development dependencies - test, lint, docs, security scan.
# Install:  pip install -r requirements-dev.txt   (pulls in requirements.txt via the -r line)

-r requirements.txt

pytest>=8.0                    # test runner 
pytest-asyncio>=0.23           # async test support
pytest-cov>=5.0                # coverage gate 
httpx>=0.27                    # AsyncClient + ASGITransport for API tests 
ruff>=0.5                      # lint + format 
pip-audit>=2.7                 # dependency CVE scan 
mkdocs-material>=9.5           # docs build 
